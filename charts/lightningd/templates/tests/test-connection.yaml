apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lightningd.fullname" . }}-test-connection"
  labels:
    {{- include "lightningd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lightningd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
